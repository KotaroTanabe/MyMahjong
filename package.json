{
  "name": "mymahjong",
  "private": true,
  "type": "module",
  "workspaces": [
    "core",
    "cli",
    "web"
  ],
  "scripts": {
    "build": "tsc -b && npm run build -w web",
    "typecheck": "tsc -b",
    "lint": "eslint .",
    "test": "npm run build && npm run test -w core && npm run test -w cli && npm run test -w web"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^8.35.0",
    "@typescript-eslint/parser": "^8.35.0",
    "@eslint/js": "^9.30.0",
    "eslint": "^9.30.0",
    "typescript": "^5.2.0"
  }
}
